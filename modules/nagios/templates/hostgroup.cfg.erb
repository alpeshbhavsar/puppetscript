define hostgroup{
        hostgroup_name  <%= @hostgroupname %> ; The name of the hostgroup
        alias           <%= @hostgroup_alias %> ; Long name of the group
        members         <%= @members_list %> ; Comma separated list of hosts that belong to this group
        }
